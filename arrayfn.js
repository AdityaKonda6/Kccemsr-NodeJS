// Array Functions
const a = [1, 2, 3, 4, 5, 6];

// Filter
const f = a.filter((a) => a > 3);
console.log(f);

// Map
const m = a.map((a) => a * 2);
console.log(m);

// Reduce
const r = a.reduce((p, a) => p + a);
console.log(r);
